<h1>Customers List</h1>

<ul>
    <%@customers.each do |c| %>
     <li> <%= link_to( c.name, customer_path(c)) %>   </li> 
   <% end %>
</ul>

   <p><%= link_to  "Create a New Customer", new_customer_path %></p>
